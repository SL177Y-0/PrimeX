// This component was archived because it depended on legacy Aries hooks/services no longer
// available in the production build. Keeping the file caused TypeScript errors during
// compilation (missing modules, implicit any). The simplified Aries dashboard lives in
// `components/aries/AriesLendDashboard.tsx`.

const UNUSED_PLACEHOLDER = true;

export default UNUSED_PLACEHOLDER;
